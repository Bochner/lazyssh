name: Setup Project Milestones

on:
  workflow_dispatch:  # Manual trigger
  push:
    paths:
      - '.github/scripts/create-milestones.py'
      - '.github/workflows/setup-milestones.yml'

jobs:
  setup-milestones:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests
      
      - name: Create milestones
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python .github/scripts/create-milestones.py
      
      - name: Create summary
        run: |
          echo "## 🎯 Project Milestones Setup Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The following milestones have been created for LazySSH development:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 Milestones Created:" >> $GITHUB_STEP_SUMMARY
          echo "1. **LazySSH v1.1 - Performance & Stability** (6 weeks)" >> $GITHUB_STEP_SUMMARY
          echo "   - SCP performance optimization" >> $GITHUB_STEP_SUMMARY
          echo "   - Transfer reliability improvements" >> $GITHUB_STEP_SUMMARY
          echo "   - Enhanced error handling" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "2. **LazySSH v2.0 - Major Release** (3 months)" >> $GITHUB_STEP_SUMMARY
          echo "   - Complete architecture modernization" >> $GITHUB_STEP_SUMMARY
          echo "   - Rich user interface" >> $GITHUB_STEP_SUMMARY
          echo "   - Advanced features" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "3. **Testing & Quality Assurance** (8 weeks)" >> $GITHUB_STEP_SUMMARY
          echo "   - Comprehensive test suite" >> $GITHUB_STEP_SUMMARY
          echo "   - Performance benchmarking" >> $GITHUB_STEP_SUMMARY
          echo "   - Security testing" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "4. **Documentation & Community** (10 weeks)" >> $GITHUB_STEP_SUMMARY
          echo "   - Complete documentation overhaul" >> $GITHUB_STEP_SUMMARY
          echo "   - Community engagement tools" >> $GITHUB_STEP_SUMMARY
          echo "   - Tutorial and demo content" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "5. **Future Enhancements** (6 months)" >> $GITHUB_STEP_SUMMARY
          echo "   - Advanced SSH features" >> $GITHUB_STEP_SUMMARY
          echo "   - Multi-server management" >> $GITHUB_STEP_SUMMARY
          echo "   - Plugin ecosystem" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🚀 Next Steps:" >> $GITHUB_STEP_SUMMARY
          echo "- Review milestone descriptions and adjust due dates if needed" >> $GITHUB_STEP_SUMMARY
          echo "- Assign existing issues to appropriate milestones" >> $GITHUB_STEP_SUMMARY
          echo "- Use milestones for sprint planning and progress tracking" >> $GITHUB_STEP_SUMMARY
          echo "- Set up project boards to visualize milestone progress" >> $GITHUB_STEP_SUMMARY 